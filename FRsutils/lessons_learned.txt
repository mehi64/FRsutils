tnorms
works on vectors
works on nxnx2 maps for fast calculations
min tnorm and product tnorm will act the same if one of the input parameters takes the binary values.

implicators work on scalar but can be vectorized with np.vectorize()

